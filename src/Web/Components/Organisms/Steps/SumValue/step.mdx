---
name: Sum Value Step
---

import { PropsTable, Playground } from 'docz'

import Step from './index'

# Sum Value Step

<Playground>
  {
    () => {
      const options = [{
          label: 'Name',
          value: 'name'
        }, {
          label: 'Home Income',
          value: 'income'
        }
      ]

      return (
        <Step
          value={5}
          options={options}
        />
      )
    }
  }
</Playground>

## Props

<PropsTable of={Step} />
